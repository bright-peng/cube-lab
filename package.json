{
  "name": "cube-lab", "version": "1.0.0", "private": true,
  "description": "Offline-first 3D Rubik's cube playground with a real seven-stage algorithm tutor.",
  "type": "module", "license": "MIT",
  "scripts": {"start": "node scripts/serve.mjs", "dev": "node scripts/serve.mjs", "build": "node scripts/build.mjs", "test": "node --test tests/*.test.mjs", "check": "node --check src/app.js && node --check src/renderer.js && node --check src/core.js && node --check src/solver.js"},
  "engines": {"node": ">=20"}
}
